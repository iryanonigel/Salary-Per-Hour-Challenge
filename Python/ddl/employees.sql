CREATE TABLE di.employees ( -- correspond with destination table (schema and table name)
	employe_id int,
	branch_id int,
	salary int,
	join_date date,
	resign_date date,
	ingesttime timestamp,
	ingest_from text
);